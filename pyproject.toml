[project]
name = "edu-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.51.0",
    "pydantic>=2.8.0",
    "rich>=13.7.0",
    "pytest>=7.4.0",
    "ruff>=0.5.0",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["src"]
